// @beecompete/config — shared config + cross-cutting types/constants.
//
// F1 skeleton placeholder. Real shared types (e.g. DTO mirrors, grade encoding
// helpers, category keys) land alongside the features that need them. Keep this
// package framework-agnostic: no React, no Next, no Spring-specific code.

export const APP_NAME = 'BeeCompete';
