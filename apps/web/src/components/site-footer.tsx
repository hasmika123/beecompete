/**
 * App-shell footer. Placeholder — real footer links (legal pages, affiliate
 * disclosure, etc.) land with R1-12.
 */
export function SiteFooter() {
  return (
    <footer className="border-t border-border">
      <div className="mx-auto max-w-6xl px-4 py-8 text-sm text-muted sm:px-6">
        <p>BeeCompete — K-12 academic-competition marketplace.</p>
        <p className="mt-1">Foundation build. Not for public use.</p>
      </div>
    </footer>
  );
}
