package com.beecompete.catalog.domain;

/**
 * The two fixed slots in the Landing "Competing changes what's possible" section (M36) — used by
 * both {@link ValuePropCard} and {@link LandingStat}. One active row per slot per table.
 */
public enum LandingSlot {
	PRIMARY,
	SECONDARY
}
